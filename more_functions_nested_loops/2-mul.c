#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first integer to be multiply
 * @b: second integer to be multiply
 *
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
