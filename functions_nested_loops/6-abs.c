#include "main.h"
/**
 * _abs - function that calculates the absolute value of an integer.
 * @v: the number to test
 * Return: -v on v
 */
int _abs(int v)
{
	if (v > 0)
	{
		return (v);
	}
	else
	{
		return (-v);
	}
}
