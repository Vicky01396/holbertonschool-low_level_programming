#include "main.h"
/**
 * _strcat - function that concatenates two strings.
 * @dest: of the overwritten string
 * @src: main string
 *
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	int h;
	int w;

	for (h = 0; dest[h] != '\0'; h++)
	{
	}
	for (w = 0; (dest[h + w] = *src++) != '\0'; w++)
	{
	}
	return (0);
}
