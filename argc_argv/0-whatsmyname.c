#include <stdio.h>
/**
 * main - prints tha name of the funtion
 * @argc: how many arguments are
 * @argv: the string
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
